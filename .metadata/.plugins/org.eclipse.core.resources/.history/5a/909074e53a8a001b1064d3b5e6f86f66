#include "filo.h"
#include <iostream>

using namespace std;

struct Node{
	int a;
	Node * next;
};

void FILO::put(int item){
	while(ptr->next != Null){
		ptr = ptr->next
	}
	Node b;
	b.a = item;
	b.next = Null;

	ptr->next = &b;
}
